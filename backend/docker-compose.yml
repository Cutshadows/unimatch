services:
  postgres:
    image: postgres:15.3
    container_name: ${CONTAINER_NAME_POSTGRES}
    restart: unless-stopped
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
      - ./db_backups:/db_backups
    networks:
      - dokploy-network
  test_db:
    container_name: test-db
    image: postgres:15.3
    ports:
      - 5433:5432
    restart: unless-stopped
    environment:
      POSTGRES_USER: centra-db-admin
      POSTGRES_PASSWORD: 12345
      POSTGRES_DB: unimatch-test-db
    volumes:
      - ./postgres_data/postgres-test-data:/var/lib/postgresql/data:rw
      - ./db_backups:/db_backups
    networks:
      - dokploy-network
networks:
  dokploy-network:
    name: dokploy-network
    driver: overlay
    attachable: true
    external: true
volumes:
  postgres_data:
    driver: local
  db_backups:
    driver: local
